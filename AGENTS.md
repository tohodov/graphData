- После завершения всех работ закоммить свои изменения в локальный git репозиторий, но не от имени глобально сохраненного пользователя а с user.name="codex", user.email="@".
- Для проверок, которым нужны Visual Studio/MSBuild/MSVC/Windows SDK или доступ к профилю пользователя, запускать команды вне sandbox под текущим пользователем через escalation. Не подменять USERPROFILE/APPDATA/HOME локальными папками репозитория ради таких проверок.
